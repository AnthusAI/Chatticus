{
  "id": "chatticus-6bd49cf4-c3d7-43c6-8392-e89c2bb0d024",
  "title": "SSE stream polls Dynamo every 50ms, sends no heartbeat, and never times out",
  "description": "**Reported by a separate analysis session 2026-09-16; CONFIRMED in code by me the same day.**\n\n`stream_turn` in `python/src/chatticus/http/app.py:1752` polls in a tight loop:\n\n    while True:\n        if await request.is_disconnected(): return\n        events = state.plane.list_turn_events(tenant_id, turn_id, replay_from)\n        if not events:\n            await asyncio.sleep(0.05)\n            continue\n\nTwo separate problems.\n\n**Query rate.** 50ms means 20 `list_turn_events` calls per second per open stream — 1,200/minute — almost all returning nothing. Cost and throttling risk scale with concurrent watchers, not with actual activity.\n\n**No deadman timeout.** Nothing bounds the total lifetime. A worker that dies or hangs without emitting `turn.completed` / `turn.failed` / `turn.reconciling` leaves this looping until the client disconnects or the runtime ceiling is hit. The report's 18,000-query figure for a 15-minute ceiling is arithmetically right (20/s x 900s).\n\n## Correction to the original report\n\nIt omitted that `request.is_disconnected()` **is** checked every iteration, which does bound the ordinary \"person closed the tab\" case. The unbounded scenario is narrower than stated: a hung worker with a client still attached. Real, but not every stream.\n\n## The part nobody flagged, which I think matters most\n\nThe generator yields **nothing** while waiting. No comment frame, no keepalive. A stream that sends zero bytes for 30 seconds is exactly what an idle timeout at a CDN or load balancer terminates — so this loop actively causes the disconnects that `4be49a` then mishandles by wiping the transcript.\n\nThat makes these two cards one story: the server does not keep the connection alive, and the client gives up when it dies. A periodic SSE comment heartbeat fixes the cause; bounded reconnect on the client fixes the symptom. Doing only the client half leaves us reconnecting constantly for no reason.\n\n## Suggested direction\n\nBack off the poll when idle rather than holding 50ms indefinitely; emit a heartbeat comment on an interval comfortably under the shortest idle timeout in front of this; and give the loop a maximum lifetime after which it closes deliberately with a reason the client can act on.\n\n## Done when\n\nAn idle stream issues far fewer queries per minute than 1,200 without delaying real events perceptibly, a turn with a long silent tool call survives without the connection being dropped, and a hung worker cannot hold a stream open indefinitely.",
  "type": "bug",
  "status": "open",
  "priority": 1,
  "assignee": null,
  "creator": null,
  "parent": "chatticus-20e38b2e-4f36-4a2b-bdfc-81917d4ca42c",
  "labels": [
    "backend"
  ],
  "dependencies": [],
  "comments": [
    {
      "id": "b0bf31ba-69f5-4b9a-b8b2-3aee77bfb2d8",
      "author": "home",
      "text": "First attempt REJECTED and closed unmerged: PR #361. Recording why, so the next attempt does not repeat it.\n\n**Wrong base.** The branch was cut from `main`, 349 commits behind `develop`. Cherry-picking it onto develop conflicts in `python/src/chatticus/http/app.py`, which has changed substantially since. Every test result reported on that branch ran against two-week-old code and proved nothing about develop.\n\nWorth noting for my own accuracy: I first read `git diff develop branch` and alarmed that the PR deleted 474 files. That was a two-dot diff — it shows how the branches DIFFER, not what merging does. GitHub reported the PR as 4 files, +311/-3, and a squash would have applied only that. Merging would not have deleted anything. The wrong base was still disqualifying, for the reason above.\n\n**The deadman timeout was backwards and caused a worse bug than the one it fixed.** It used `state.plane.turn_deadline` (120s), documented as \"how long an active turn may run WITHOUT RENEWAL\" — a lease-renewal deadline, not a maximum duration; `handle_turn_deadline` exists to renew it. Used as an absolute stream lifetime, every turn longer than two minutes had its stream closed while perfectly healthy — exactly the slow-tool-call case the heartbeat exists to protect. The heartbeat would hold the connection open for 120 seconds and then kill it deliberately. It also closed with a bare `return`, so on the client (4be49a) `parkStreamWithoutTerminal` fires and wipes the user's in-progress answer.\n\n**Correct shape, for the next attempt:** an INACTIVITY timeout, not total-elapsed. Close only when no real events have arrived for the window; reset on every yielded event; the heartbeat must NOT reset it, or the deadman never fires. An actively streaming turn must never be closed regardless of duration. Emit a terminal event carrying a reason before returning, never a bare return.\n\n**The backoff test had no teeth** — the attempt's own notes said disabling backoff left it passing. Two other checks depended on a real 30-second read timeout and on a test that hung rather than failed; a hanging test hangs CI. All timing must be driven by injected clocks.\n\nRe-dispatched with these constraints. The first re-dispatch died on a quota limit before doing any work.\n",
      "created_at": "2026-09-16T20:52:41.832773Z",
      "comment_type": "default",
      "agent": {
        "platform": "claude-code",
        "model": "claude-opus-5",
        "name": "Chatticus"
      }
    }
  ],
  "created_at": "2026-09-16T16:48:25.092398Z",
  "updated_at": "2026-09-16T20:52:41.832792Z",
  "closed_at": null,
  "agent": {
    "platform": "claude-code",
    "model": "claude-opus-5",
    "name": "Chatticus"
  },
  "right_now_summary": null,
  "right_now_updated_at": null,
  "custom": {}
}