{
  "id": "chatticus-9a07e7c2-f7e4-46ea-b400-477b8ce4fe52",
  "title": "Decide how a customer's bot gets authority beyond read_workspace",
  "description": "**Phase 1 of 5.**\n\n- **Blocked by:** nothing\n- **Surfaces from:** `chatticus-5336ff` (grants attach on first human message) and `chatticus-e11c17` (terminal tool)\n\n---\n\n## The question\n\n**How does a customer's bot ever get more authority than `read_workspace`?**\n\n`household_conversation_grant` (`capability_policy.py:138`) is the grant attached when a human starts a bot turn:\n\n```python\ntools=frozenset({\"read_workspace\"}),\norigins=frozenset(),\nrecipients=frozenset(),\nfile_scopes=frozenset({\"/workspace\"}),\negress_classes=frozenset({APPROVED_ORIGIN_FETCH}),\ningest_classes=frozenset(),\n```\n\n**One tool.** Verified live on development by `chatticus-5336ff` — a human message to Ping returned the household preset, no browse, write, send, or purchase.\n\n## Why this matters now\n\nCapabilities are being proven through kernel paths with explicit grants. That is the right way to de-risk, and it means **\"capability proven\" and \"a customer's bot can use it\" have become different claims.** The capability matrix records the first.\n\n| Capability | Proven | In the default grant |\n|---|---|---|\n| `read_workspace` | yes | **yes** |\n| `write_workspace` | yes — live recycle, `chatticus-863f27` | **no** |\n| `browser_open` | yes — kernel deviation, `chatticus-8fe4c7` | **no** |\n| `run_terminal` | pending `chatticus-e11c17` | **no** |\n\nOn current code a customer's bot can do exactly one thing: read files under `/workspace`. Everything else on that list was proven by an operator with a widened grant.\n\n## What exists, and what appears not to\n\n**Exists:** `task_grant_for_requested_capability` (`authorization_ceiling.py:51`) and a `PUT /turns/{turn_id}/grant` route (`http/app.py:1550`).\n\n**Appears not to:** a customer-facing path to that route. It is registered on the **worker** router, and no flow in the web app was found that widens a grant. **This is the part to confirm rather than take from this card** — if such a path exists, this card closes as already-answered.\n\n## The decision, if it does not exist\n\nThree shapes, and they are genuinely different products:\n\n1. **Per-task human grant.** A bot requests, a human approves, authority is scoped to that task. Matches the delegation-ladder positioning and the existing approvals machinery. Needs a customer-facing surface that does not exist.\n2. **Per-bot standing grant.** A human configures what a bot may do once, at creation or in settings. Simpler, coarser, and it needs the create-bot UI that `chatticus-7622fd` is already about.\n3. **Widen the default preset.** Cheapest, and it moves authority from deliberate to ambient — which is the opposite of what the pitch page argues. Probably wrong for `run_terminal` specifically; arguably fine for `write_workspace`, since a bot that can read but not write its own workspace is a strange product.\n\n## Not a defect\n\nA minimal default is **correct**. `chatticus-8a25af` sets the standard that every permission should survive *\"why does an agent product need this,\"* and a bot that can open a shell on a customer's machine by default would not. The gap is that the deliberate-grant path is unbuilt, so the practical answer today is \"it does not get more authority.\"\n\n## Done when\n\nEither a customer-facing grant path is identified and recorded, or one of the three shapes above is chosen and its card exists. Also record whether `write_workspace` belongs in the default preset — it is the one that looks most like an accident rather than a policy.",
  "type": "story",
  "status": "closed",
  "priority": 0,
  "assignee": null,
  "creator": null,
  "parent": "chatticus-598be488-151d-4e57-8897-5cf6aaed50e5",
  "labels": [
    "phase-1"
  ],
  "dependencies": [],
  "comments": [
    {
      "id": "50ff1f96-67f3-45cd-9f9b-8ea1a324ee42",
      "author": "home",
      "text": "Confirmed: there is **no** customer-facing grant-widening path. `PUT /orgs/{tenant}/turns/{id}/grant` is on `worker_router` (`enforce_worker_principal`). Gherkin (`thin_grant_http_steps.py`) uses worker auth headers. `web/` has zero grant references. `task_grant_for_requested_capability` builds a grant from a sink request to compare against a member ceiling — it is not an API that gives a bot more tools.\n\nThe gap stands. Do not close this card as already-answered. Do not pick one of the three shapes from this comment. `write_workspace` still looks like the accidental omission vs `run_terminal`.\n",
      "created_at": "2026-09-08T14:14:02.526013Z",
      "comment_type": "default"
    },
    {
      "id": "7050428f-5327-4601-bd50-320d76672221",
      "author": "home",
      "text": "Composer 2.5 planning only. Confirmed: no customer-facing grant-widening path (`PUT /turns/{id}/grant` is worker-only). Worktree to be named `/tmp/chatticus-9a07e7-grant-authority` on `feat/9a07e7-grant-authority` from `origin/develop`. Implementation not authorized until the plan picks **one** of the three shapes (per-task human grant, per-bot standing grant, or widen the default) and says what happens to `write_workspace`. Do not widen `run_terminal` into `household_conversation_grant()` in this card.\n",
      "created_at": "2026-09-08T15:39:09.484274Z",
      "comment_type": "default"
    },
    {
      "id": "b26c4397-7aa6-4032-9716-cc60ee9115ac",
      "author": "home",
      "text": "Decision authorized.\n\n**Shape 1** — per-task human grant. Not standing-per-bot. Not “widen the default” as the product answer.\n\n**`write_workspace` IN** the conversation preset (baseline correction). **`run_terminal` OUT.**\n\n**Amendment on authorize:** customer widening is **replace** with a full closed grant, same as `Given a human task grants:`. Do not merge/union with the conversation preset.\n\nChildren (parent epic `598be4`):\n- ID: chatticus-0ee0db\nTitle: Conversation preset includes write_workspace\nType: story\nStatus: open\nPriority: 0\nAssignee: -\nParent: chatticus-598be488-151d-4e57-8897-5cf6aaed50e5\nLabels: phase-1\nRight now: (no right-now summary)\nDescription:\n**Phase 1.** Parent epic `chatticus-598be4`. Child of the `chatticus-9a07e7` decision (Shape 1: per-task grant; conversation baseline is a correction, not Shape 3).\n\n## Decision already made\n\n`household_conversation_grant()` currently has only `read_workspace`. A bot that can read `/workspace` but not write it is an accident, not a policy. Add **`write_workspace`** to the preset. Do **not** add `run_terminal`, `browse`, `send`, or `purchase`.\n\nFile scopes stay `/workspace`. Egress stays `approved_origin_fetch`.\n\n## Constraints\n\n- One path. Change `household_conversation_grant()` only. No dual readers.\n- Gherkin first: extend `conversation_turn_grant.feature`.\n- Explicit `Given a human task grants:` still **replaces** the preset (no merge).\n- Not the customer-facing widen HTTP (`9a07e7` Card B).\n\n## Done when\n\nA human-started turn's grant includes `write_workspace` and `read_workspace`. Live on development: Dynamo grant tools list both. `run_terminal` still absent from the preset. — preset + `write_workspace`\n- ID: chatticus-be3997\nTitle: Enabled member replaces an active turn grant over HTTP\nType: story\nStatus: open\nPriority: 0\nAssignee: -\nParent: chatticus-598be488-151d-4e57-8897-5cf6aaed50e5\nLabels: phase-1\nRight now: (no right-now summary)\nDescription:\n**Phase 1.** Parent epic `chatticus-598be4`. Child of the `chatticus-9a07e7` decision: Shape 1, per-task human grant.\n\n## What to ship\n\nAn **enabled member** (Cognito user principal) can attach a closed `TaskCapabilityGrant` to an **active turn**. Clip to that member's ceiling. Worker `PUT /turns/{id}/grant` stays worker-only.\n\n**One path: replace, not merge.** The body is a full closed grant (same as `Given a human task grants:`). It replaces `household_conversation_grant()` for that turn. Do not union tools with the conversation preset.\n\n## Constraints\n\n- Gherkin first (`turn_grant_widening.feature` or equivalent).\n- New route on **`user_router`**, not `worker_router`.\n- Deny when the grant exceeds member ceiling.\n- Do not put `run_terminal` in the conversation preset.\n- No web UI in this slice (Card C).\n\n## Done when\n\nAn authenticated org member can replace the turn grant over HTTP; an unauthenticated caller cannot; a grant that exceeds ceiling is refused; worker PUT remains worker-only. — user HTTP replace\n- ID: chatticus-753bd8\nTitle: Workspace UI to replace an active turn grant\nType: story\nStatus: open\nPriority: 1\nAssignee: -\nParent: chatticus-598be488-151d-4e57-8897-5cf6aaed50e5\nLabels: phase-1\nRight now: (no right-now summary)\nDescription:\n**Phase 1.** Parent epic `chatticus-598be4`. Blocked by user-facing turn grant HTTP (the `9a07e7` control-plane child).\n\n## What to ship\n\nWorkspace UI for an enabled member to submit a **full closed grant** (replace, not merge) for the active turn. Distinct from consequential send/purchase approval.\n\n## Constraints\n\n- Gherkin first. Depends on the user HTTP route existing.\n- Do not invent a second grant model.\n- `run_terminal` is offered only as an explicit grant tool, never as a default checkbox that silently lands in the conversation preset.\n\n## Done when\n\nA member can widen (replace) the active turn grant from the product workspace without the members CLI or kernel. — workspace UI\n- ID: chatticus-7fe070\nTitle: Per-task run_terminal via human grant replace\nType: story\nStatus: open\nPriority: 1\nAssignee: -\nParent: chatticus-598be488-151d-4e57-8897-5cf6aaed50e5\nLabels: phase-1\nRight now: (no right-now summary)\nDescription:\n**Phase 1.** Parent epic `chatticus-598be4`. Ties `chatticus-e11c17` (`run_terminal` tool) to Shape 1: per-task human grant.\n\n`run_terminal` must **not** enter `household_conversation_grant()`. A customer bot gets a shell only when a human replaces the turn grant with one that includes `run_terminal` (and file_scopes for cwd).\n\nDepends on user-facing grant HTTP (and the web surface if that is how the human submits). Live `e11c17` host proof may still be blocked on `HOST_NOT_RUNNING`; this card is the **customer authority path**, not another kernel deviation.\n\n## Done when\n\nGherkin shows a human-widened turn can `run_terminal` and a conversation-preset turn cannot. Do not live-poll a stopped host to rediscover `HOST_NOT_RUNNING`. — `run_terminal` only via replace\n\nThis card is the decision. Closing.\n",
      "created_at": "2026-09-08T15:41:24.553224Z",
      "comment_type": "default"
    },
    {
      "id": "b413cadf-64c9-44d2-b18a-343352d11125",
      "author": "home",
      "text": "Children (clean ids): `chatticus-0ee0db` preset write_workspace; `chatticus-be3997` user HTTP replace; `chatticus-753bd8` workspace UI; `chatticus-7fe070` per-task run_terminal. Planner worktree `/tmp/chatticus-9a07e7-grant-authority` should be removed now that this card is closed.\n",
      "created_at": "2026-09-08T15:42:55.609773Z",
      "comment_type": "default"
    },
    {
      "id": "9ebc5c47-fea6-41ad-8924-b45db8108b57",
      "author": "home",
      "text": "## Answered — and one correction to this card (2026-09-08)\n\n**I was wrong about the route.** This card says `PUT /turns/{turn_id}/grant` is \"registered on the **worker** router\" with no customer-facing flow. It is on `user_router` (`http/app.py:1550`), behind `RequireUserPrincipal`. I flagged that as the part to confirm rather than trust, and confirming it falsified it. Whether it moved in #331/#333 or I misread, the card's premise was wrong on that point.\n\n**The decision landed as option 1 — per-task human grant — with the surface built.**\n\n- `household_conversation_grant` is now `{read_workspace, write_workspace}` (#330). `write_workspace` was the one that looked like an accident rather than a policy; it is now deliberate.\n- `run_terminal` stays **out** of the default and **on** the authorization form. That is the right split: a shell on a customer's machine is granted per turn by a human, never ambiently.\n- `TurnGrantPanel` + `web/lib/turn-grant.ts` are the customer surface (#333).\n\n## Why the implementation holds up\n\nChecked rather than assumed, because a grant UI is a confused-deputy target:\n\n- **Tool names are hardcoded literals** driven by boolean checkboxes, not strings from bot or page content. Nothing a bot says can inject a tool into its own grant.\n- **`recipients: []` is hardcoded**, so send is structurally impossible from the form. Purchase is absent entirely.\n- **Browse without named origins returns `null`** — no wildcard browse grant.\n- **The confirmation text is built from the sorted tools array**, so the human is shown the structured grant rather than any authored prose.\n- **The panel is visible only while the turn is ACTIVE.**\n\nAnd the server does not trust the client:\n\n- The turn must be ACTIVE, else `TurnTerminalError`.\n- The actor must have standing, else `MemberStandingRequiredError`.\n- **`grant_replace_exceeds_acting_member_standing` rejects any grant exceeding the acting member's own ceiling, checked per tool.** A human cannot grant a bot more authority than they hold — no privilege escalation through delegation.\n- The full grant plus `actor_user_id` is appended as a turn event. Audited.\n\n## One question left, and it is about the half that is missing\n\n`PUT` is replace semantics, so **empty tools is a revocation** — the API returns 200 and the panel blocks it.\n\nBlocking accidental empty submits is right. But revocation is arguably the more important half of a delegation model: the moment you most want it is when a turn is going wrong and authority needs pulling back mid-turn. The API supports that today and the UI does not expose it.\n\nWorth deciding deliberately: is mid-turn revoke a feature to surface, or is ending the turn the intended escape hatch? If the latter, say so — otherwise the capability exists, is unreachable, and someone will rediscover it.\n\n**Suggest closing this card** once the live Dynamo check on `chatticus-0ee0db` confirms the preset, with the revoke question carried forward separately.",
      "created_at": "2026-09-08T18:25:32.731034Z",
      "comment_type": "default"
    }
  ],
  "created_at": "2026-09-08T14:10:17.587933Z",
  "updated_at": "2026-09-08T18:25:32.731075Z",
  "closed_at": "2026-09-08T15:41:24.660555Z",
  "right_now_summary": null,
  "right_now_updated_at": null,
  "custom": {}
}