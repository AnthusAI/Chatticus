{
  "id": "chatticus-ea918acd-69a6-49e3-879e-62088ff31ddc",
  "title": "Resolve the actimeo vs concurrent-workspace contradiction",
  "description": "## The contradiction\n\nTwo decisions recorded on `chatticus-fbae4e` cannot both hold as written. The 2026-09-05 campaign created the conflict by answering `chatticus-766c51`; neither decision has been updated for it.\n\n**D5 says** a long `actimeo` is *\"the load-bearing mitigation\"* for WAN metadata latency, and the 2026-09-05 correction justifies it as safe **\"only because of the single-writer-per-workspace scoping.\"**\n\n**`766c51` found** that turns are **not** serialized per bot, and the initiative's own default is one workspace per bot. So the default configuration has **concurrent turns sharing one workspace** — precisely the condition under which a long `actimeo` is not safe.\n\n## Why \"NFS arbitrates concurrent writers\" does not answer it\n\nThe concurrency section on `fbae4e` resolves this with *\"under EFS (D1/D3), NFS arbitrates concurrent writers.\"* That is true for **writers** and does not cover the failure mode:\n\n- **Byte-range locking prevents corruption.** It says nothing about attribute-cache staleness.\n- **Close-to-open consistency does not rescue it**, because `git status` calls `lstat()` — it does not `open()` the files. Cached attributes and cached directory entries are trusted for up to `acregmax` / `acdirmax` without revalidation.\n\n**The failure mode is staleness, not corruption.** Turn A writes a file; turn B runs `git status`; for up to `acdirmax` seconds turn B does not see it. In an agent workflow where B consumes A's output, that presents as intermittent flakiness — the hardest class of bug to attribute.\n\n**Precision that bounds the blast radius:** two turns sharing *one NFS client* (same host, same mount) are coherent — one cache. The hazard requires concurrent turns for one bot placed on **different hosts** sharing a workspace, which today's scheduler permits.\n\n## This is not only a local-interactive-host problem\n\nThe epic records EFS-for-AWS-hosts as having **\"no unknowns\"** blocking it. That is still true of *experiments*, but this design question now attaches to it:\n\n- The local interactive host makes it worse (long `actimeo` deliberately widens the window).\n- **AWS hosts have the same exposure at default `actimeo`** — a smaller window, tens of seconds, but nonzero — whenever concurrent turns for one bot land on different hosts.\n\nSo this is not gated behind `9af381`. It applies to the track we called ungated.\n\n## Qualification to D3\n\nD3 says the lease and its machinery exist only because S3 lacks locking, and that under EFS *\"the two-writer problem stops existing.\"*\n\nThat holds for **write arbitration**. It does not hold for **cache coherence**. If the resolution below requires serializing turns per workspace, something lease-shaped returns — at the scheduling layer rather than the storage layer, and for a different reason. D3 should be qualified either way, even if the resolution avoids a lease.\n\n## Options to decide between\n\n| Option | Mechanism | Cost |\n|---|---|---|\n| **A. Per-turn workspace** | Every concurrent turn declares its own `workspace_id`; single-writer holds; long `actimeo` is safe | Turns cannot share a tree — which is sometimes exactly what is wanted |\n| **B. Short `actimeo`** | Accept default cache timeouts | D5's load-bearing mitigation is gone; the local interactive host likely fails its own threshold table |\n| **C. Explicit share policy** | Workspace declares shared vs exclusive; exclusive gets long `actimeo`, shared gets short | Honest, more surface |\n| **D. Serialize turns per workspace** | Scheduler-level, not storage-level | Reintroduces lease-shaped coordination that D3 claims is unnecessary |\n\n## Consequence for `workspace_id`\n\nThe initiative currently frames `workspace_id` as the **product isolation** boundary. Under any resolution but B it is also load-bearing for **cache-coherence correctness**, which is a stronger requirement than a convenience default.\n\n## Not an experiment\n\nThis is a decision, not a measurement. It needs no AWS and no spike — it needs the four options above resolved and D5, D3, and the concurrency section updated to agree with each other.\n\n## Done when\n\nD5, D3, and the concurrency section on `chatticus-fbae4e` state one consistent position, and `workspace_id`'s role is recorded as isolation-plus-coherence or explicitly not.",
  "type": "task",
  "status": "closed",
  "priority": 1,
  "assignee": null,
  "creator": null,
  "parent": "chatticus-e9ddec0c-76bb-4acb-81a7-0afee00e3dea",
  "labels": [
    "architecture",
    "storage"
  ],
  "dependencies": [],
  "comments": [
    {
      "id": "2740e821-47ca-4a95-b79e-ecaccc69e857",
      "author": "home",
      "text": "Do not use the first 9af381 Phase A0 “~32s staleness across all actimeo” as decision input. The observer was `git status` (~30s), so the curve is the metadata op cost, not cross-client attribute-cache delay. A `test -f`/`stat` probe is in flight on PR #310.",
      "created_at": "2026-09-05T22:23:57.236449Z",
      "comment_type": "default"
    },
    {
      "id": "7974afe0-a82e-40c3-b362-13a532da7dd3",
      "author": "home",
      "text": "Phase A0 stat-probe (PR #310): cross-client create visible in ~0.75–0.83s for every `actimeo` (1–300s). Git-status contrast ~60s is the op cost, not cache delay. The ~0.8s floor is not a clean `actimeo` curve (possible docker-exec after write). Performance vs freshness is **not** a tradeoff in this lab: long `actimeo` did not make `git status` faster. Options A–D still need a product decision; this spike does not pick one.",
      "created_at": "2026-09-05T23:28:32.063937Z",
      "comment_type": "default"
    },
    {
      "id": "e72e4197-04ce-40d6-9f85-cd06af300f98",
      "author": "home",
      "text": "## The A0 staleness curve does not settle this (2026-09-07)\n\nA0 reported \"performance vs freshness is **not** a tradeoff in this lab: long `actimeo` did not make `git status` faster,\" which would dissolve this contradiction if true. It is not established.\n\nEach timed iteration rewrites the tree (`git checkout bench-a && git checkout bench-b` between the measurements), so the cache is invalidated every pass regardless of `actimeo`. Flat ratios across actimeo 1–300s are what that workload produces by construction. The server was also nfs-ganesha in Docker, carrying ~13x versus local on LAN.\n\nSo the premise this task turns on — that a long `actimeo` buys performance at the cost of cross-client staleness — is **neither confirmed nor refuted**. The stat-probe staleness floor (~0.75–0.83s, flat across actimeo) was already flagged as possibly docker-exec overhead rather than a measured cache window.\n\n**This decision still needs making, and it is not only about the dead local host.** It applies to AWS hosts at default `actimeo` whenever concurrent turns for one bot land on different hosts — which `766c51` established the scheduler permits. Two turns on one NFS client share a cache and are coherent; different hosts are not.\n\n`chatticus-3d5357` includes a read-only `actimeo` sweep on real EFS, which would produce the curve this task actually needs. Options A–D still require a product decision either way.",
      "created_at": "2026-09-07T12:51:14.175725Z",
      "comment_type": "default"
    },
    {
      "id": "18acad1d-c89e-4c3d-bb79-ad61a848e555",
      "author": "home",
      "text": "## Decision (2026-09-07) — option B for remaining EFS\n\n`chatticus-3d5357` removed the premise this card was built on. `/workspace` is **not** on EFS (mutating 23–54× vs EBS). There is no NFS hot tree, so there is no long-`actimeo` vs concurrent-workspace-on-two-clients tradeoff for git metadata.\n\n**Pick B:** any EFS mount (`/org` only) uses **default / short `actimeo`**. Do not set a long attribute-cache timeout to \"save\" metadata-heavy work — 3d5357 showed that does not get a tree under 3×, and `actimeo=1` thrashes.\n\n| Option | Why not |\n|---|---|\n| A per-turn workspace | Isolation remains a product feature; it is not required to make NFS cache coherent for a working tree that is not on NFS |\n| C share policy | Extra surface; no EFS `/workspace` to special-case |\n| D serialize / lease-shaped | Do not bring a scheduler lease back for NFS attribute cache. D3 stays: POSIX locking for `/org` writers. Snapshot/hydrate for a **local** `/workspace` is a different question (host mobility), not this card |\n\n**`workspace_id`:** product isolation boundary, **not** an NFS coherence lever.\n\nD5, D3 (write arbitration vs cache), and the concurrency section on `fbae4e` now agree: no long `actimeo`; NFS staleness on `/org` is accepted at default timeouts; hot tree is local. Closing.",
      "created_at": "2026-09-07T14:25:00.603621Z",
      "comment_type": "default"
    }
  ],
  "created_at": "2026-09-05T20:34:06.008440Z",
  "updated_at": "2026-09-07T14:25:00.616909Z",
  "closed_at": "2026-09-07T14:25:00.616909Z",
  "custom": {}
}