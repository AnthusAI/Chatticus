{
  "name": "chatticus-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Chatticus AWS CDK app. All AWS resources are defined here.",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "synth": "cdk synth",
    "synth:customer-computers": "cdk --app 'npx ts-node bin/customer-computers.ts' synth ChatticusComputers",
    "check:customer-computers-template": "node scripts/check-customer-computers-template.mjs",
    "test": "tsx --test test/**/*.test.ts",
    "deploy": "echo 'Refuse: cdk deploy --all would touch snapshots, computers, and every thin-turn environment. Deploy one stack, e.g. npx cdk deploy ChatticusThinTurn' >&2; exit 1",
    "diff": "cdk diff"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.215.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "aws-cdk": "^2.215.0",
    "tsx": "^4.20.5",
    "typescript": "^5.7.3"
  }
}
