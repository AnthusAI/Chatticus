name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: read

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install JS dependencies for auth behavior harness
        run: npm ci

      - name: Install
        working-directory: python
        run: pip install -e ".[dev]"

      - name: Formatter and linter versions
        working-directory: python
        run: |
          python -c "import black; print('black', black.__version__)"
          ruff --version

      - name: Behave
        working-directory: python
        run: behave

      - name: Pytest
        working-directory: python
        run: pytest

      - name: Format
        working-directory: python
        run: black --check src ../features tests

      - name: Lint
        working-directory: python
        run: ruff check src ../features tests

  infra:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install
        run: npm ci

      - name: Test
        working-directory: infra
        run: npm test

      - name: Check customer-computers template artifact
        working-directory: infra
        run: npm run check:customer-computers-template

      - name: Synth
        working-directory: infra
        env:
          CHATTICUS_STUB_WEB_BUNDLE: "1"
        run: npx cdk synth
